[project]
name = "pr"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
managed = true
default-groups = ["dev"]

[dependency-groups]
dev = [
    "ruff>=0.9",
]
